# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Data & build artifacts (source data is downloaded, not committed)
data/
spark-warehouse/
# Bronze tables built from the test fixtures, by CI or by a local repro of it.
ci-warehouse/
output/
*.duckdb

# Spark
metastore_db/
derby.log

# dbt
target/
dbt_packages/
logs/

# Tooling
.pytest_cache/
.ruff_cache/

# Local Claude Code session state (settings, worktrees)
.claude/

# Scratch notes, not part of the project
comprehension.txt

# dbt local state
dbt/.user.yml

# Generated dashboards. The script regenerates them; committing one would
# be an artifact nothing regenerates (ADR 0002).
viz/out/
